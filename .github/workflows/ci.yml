name: CI

on:
  pull_request:
  push:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  required:
    name: Required / Bun 1.3.14
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Set up Bun
        id: setup-bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.14

      - name: Record Bun version and revision
        env:
          SETUP_BUN_VERSION: ${{ steps.setup-bun.outputs.bun-version }}
          SETUP_BUN_REVISION: ${{ steps.setup-bun.outputs.bun-revision }}
        run: |
          bun_version="$(bun --version)"
          bun_revision="$(bun --revision)"
          printf 'bun --version: %s\n' "$bun_version"
          printf 'bun --revision: %s\n' "$bun_revision"
          printf 'setup-bun version: %s\n' "$SETUP_BUN_VERSION"
          printf 'setup-bun revision: %s\n' "$SETUP_BUN_REVISION"
          {
            printf '### Required Bun runtime\n\n'
            printf -- "- \`bun --version\`: \`%s\`\n" "$bun_version"
            printf -- "- \`bun --revision\`: \`%s\`\n" "$bun_revision"
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Typecheck
        run: bun run typecheck

      - name: Lint
        run: bun run lint

      - name: Test
        run: bun run test

  canary:
    name: Compatibility / Bun canary
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Set up Bun
        id: setup-bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: canary

      - name: Record Bun version and revision
        env:
          SETUP_BUN_VERSION: ${{ steps.setup-bun.outputs.bun-version }}
          SETUP_BUN_REVISION: ${{ steps.setup-bun.outputs.bun-revision }}
        run: |
          bun_version="$(bun --version)"
          bun_revision="$(bun --revision)"
          printf 'bun --version: %s\n' "$bun_version"
          printf 'bun --revision: %s\n' "$bun_revision"
          printf 'setup-bun version: %s\n' "$SETUP_BUN_VERSION"
          printf 'setup-bun revision: %s\n' "$SETUP_BUN_REVISION"
          {
            printf '### Canary Bun runtime\n\n'
            printf -- "- \`bun --version\`: \`%s\`\n" "$bun_version"
            printf -- "- \`bun --revision\`: \`%s\`\n" "$bun_revision"
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Typecheck
        run: bun run typecheck

      - name: Lint
        run: bun run lint

      - name: Test
        run: bun run test
